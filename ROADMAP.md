# 多多朗读 · 版本规划（ROADMAP）

> 本文件记录已确认但**暂不实行**的下版本改进需求，实装时逐项勾选并同步到 version_info.py。

## v1.97 改进计划

- [x] **新增阅读主题「米黄·截图款」**：按用户提供的小说 App 截图提取，`{"bg": "#DCD1B5", "fg": "#000000", "hl": "#E8C87A"}`，加入 THEMES（拟命名「米黄」或「羊皮纸」）
- [x] **更换软件图标**：用户已选定 **OpenRunde·白色DD**（`_icon_candidates/icon5_1.png`，`#202020` 圆角背景 + OpenRunde 字体白色实心双 D），替换 `assets/app.ico`（需转多尺寸 .ico：16/32/48/64/128/256）
- [x] **功能区按钮美化（方案D·原生微调）**：不切换主题，`ttk.Style.configure("TButton", padding=(8,5), relief="flat", font=("微软雅黑",9))` + `map` 加悬停 `#E5F1FB`/按下 `#DCEBF7`，统一间距圆角，最贴近系统观感（详见 `功能区美化预览.html` 方案 D）
- [x] **底部进度条同步美化（方案D风格）**：底部阅读进度滑块 `Seek.Horizontal.TScale`（当前 troughcolor `#d8d8d8`/background `#000000`）与方案 D 统一——槽色微调为 `#E0E0E0`、滑块用系统默认灰、保持原生圆角高度，悬停视觉与按钮一致（不切换主题）
- [x] **其余 ttk 控件统一（方案D延伸）**：用 `ttk.Style` 一次性配置——`TCombobox`×5（字体微软雅黑9/边框/下拉箭头/悬停）、`TProgressbar`×3（trough 浅灰+进度色）、`Treeview`（表头/选中色/行高/字体）、`TNotebook`（标签页字体与选中高亮）、`TPanedwindow`（分隔条颜色）、`TSeparator`（分隔线颜色统一）
- [x] **tk 原生控件统一（方案D延伸）**：`tk.Menu` 右键菜单×2（字体微软雅黑/悬停高亮）、`tk.Checkbutton`（整本缓存自动关机，选中样式）、`tk.Listbox`×2（章节目录/缓存章节选择，字体/选中色）、滚动条统一
- [x] **对话框与状态栏统一（方案D延伸）**：所有 Toplevel 弹窗（导入/重复确认/整本缓存/定时/关于）背景色、标题、按钮字体统一；关于对话框白底与缓存页 `#f5f6f8` 合并统一；状态栏彩色标签（`#3366aa`/`#8a5a00`/`#c0392b`）确认与浅灰系协调并统一字体
- [x] **书架区默认关闭**：开启软件后书架区默认收起，不占用界面
- [x] **书架区布局调整**：书架区往下移动，与目录区同层、与阅读区齐平（避免书架区独立占满顶部导致功能区错位）
- [x] **防挤占修复**：解决开关书架区时挤占顶部功能区按钮、导致按钮显示不全的问题
- [x] **关于界面图标彩蛋 + 皮肤模式**：关于对话框头部显示当前图标（64x64），点击进入「皮肤模式」窗口，27 款图标方案网格展示，点击任意图标一键切换软件图标（PNG 转多尺寸 ico 保存到 %APPDATA%\DDNovelReader\custom.ico，即时更新窗口图标，重启后任务栏/快捷方式同步）

> 主题与 UI 布局两项实装时需一起回归测试（窗口最大化/缩放/字号变化下均正常）。


---

## v1.97 实装状态（2026-09-01）

全部 11 项已实装并打包为 `releases/v1.97/多多朗读_v1.97.exe`（45.1MB）。
- 米黄主题已加入 THEMES（主题下拉框可切换）
- 图标已替换为 OpenRunde·白色DD（assets/app.ico，多尺寸 16-256）
- 全控件方案D统一美化（clam 主题 + 浅灰系 + 悬停 #E5F1FB）
- 书架重构为内层 PanedWindow（与目录/阅读区同层），默认关闭，防挤占
- 皮肤模式 27 款图标可切换（需 PIL，已打包）
